# blockchain

Para correr:

1. Frontend
  ejecutar
    yarn install (para instalar los modulos)
    yarn start (para correr la app)
    
2. Backend
  Ejecutar
    pip install -r requirements
    flask run --port 8000
  
